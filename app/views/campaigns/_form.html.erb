<%= form_for(@campaign) do |f| %>
  <% if @campaign.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@campaign.errors.count, "error") %> prohibited this campaign from being saved:</h2>

      <ul>
      <% @campaign.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :objective %><br>
    <%= f.text_field :objective %>
  </div>
  <div class="field">
    <%= f.label :campaign_group_status %><br>
    <%= f.text_field :campaign_group_status %>
  </div>
  <div class="field">
    <%= f.label :buying_type %><br>
    <%= f.text_field :buying_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
